// OperationResult.js
import React from 'react';

function OperationResult({ number1, number2, operation }) {
  let Result = 0;

  switch (operation) {
    // Calculation logic
  }

  return <div className="output">Result is: {Result}</div>;
}

export default OperationResult;
